{% trans_default_domain 'FOSUserBundle' %}
<a style="margin-left: 5%" href="{{ path('game_new') }}">Upload new game image</a>
<div style="width: 45%; margin: 5%">


    <h2>{{ user.username }}</h2>
        <table class="table table-condensed">
            <tbody>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>First name</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Last name</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Last name</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>
                <a href={{ path('current_user_games') }}>See uploaded games</a>
                </th>
                <td></td>
            </tr>
            <tr>
                <th>
                    <a href={{ path('', { 'id': user.id }) }}>
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                    </a>
                </th>
                <td></td>
            </tr>
            </tbody>
        </table>
</div>
